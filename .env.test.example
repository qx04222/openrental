# Copy to .env.test. Point at a throwaway database — the e2e harness truncates it.
DATABASE_URL=postgresql://postgres:password@localhost:5432/openrental_test
APP_TIMEZONE=America/Toronto
NODE_ENV=test
